/**
 * Twitter Card image — re-exports the OG image with Twitter-specific metadata.
 * Uses the same 1200x630 dimensions which work for both OG and Twitter.
 */
export { default, alt, size, contentType } from './opengraph-image'
